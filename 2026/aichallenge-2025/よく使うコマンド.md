## ✅ よく使うコマンド
### 🎯 AIチャレンジコード実行
#### Dockerコンテナのビルド＆起動初回
```bash
cd data/aichallenge/2026/aichallenge-2025
cd data/aichallenge/aichallenge-2025_3rd/aichallenge-2025
./docker_build.sh dev
docker images

./docker_run.sh dev cpu     # GPUなし
ls

cd /aichallenge
./build_autoware.bash
```
#### Dockerコンテナ上でのAutowareとSimulatorの実行
```bash
./run_evaluation.bash
```

#### ビルド成果物のリセット
```bash
cd workspace
rm -rf build/ install/ log/
cd ..
```

### 🎯 関連ツール
#### trajectoryのポイントを追加・削除したい
```bash
# standalone-trajectory-editor
cd data/aichallenge/2026/standalone-trajectory-editor/build
./trajectory_editor
```

#### aichallenge-trajectory-editor
```bash
./run_simulator.bash awsim
./run_autoware.bash awsim
```
#### パラスタ値＆最新走行結果自動転機
```bash
cd data/aichallenge-2025_shio
python3 lap_time_tracker_v2.py
```

